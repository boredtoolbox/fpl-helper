# Core web app
Flask==3.1.3
requests==2.31.0
APScheduler==3.11.3
PyYAML==6.0.1

# Crowd-intelligence layer (optional at runtime — app degrades gracefully)
pydantic==2.12.5
rapidfuzz==3.14.5
feedparser==6.0.11
youtube-transcript-api==1.2.4
google-genai==2.19.0
trafilatura==2.2.0

# Production WSGI server (see the systemd unit in README.md).
# The Flask dev server also works on a private network.
waitress==3.0.2

# Tests
pytest==9.1.1
